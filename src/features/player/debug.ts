// Debug utilities for diagnosing UI/interaction issues

export const showClickMarker = (x: number, y: number) => {
    // Disabled for production
    return;
};

export const showBoxCorner = (x: number, y: number) => {
    // Disabled for production
    return;
};
